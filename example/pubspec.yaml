name: example
description: A new example project.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  unleash:
    path: ../../unleash

dev_dependencies:
  lints: ^1.0.0